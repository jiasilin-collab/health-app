{
  "name": "health-app",
  "version": "1.0.0",
  "description": "全生命周期健康管理App",
  "main": "index.js",
  "scripts": {
    "android": "cap sync android && open android",
    "build:android": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
